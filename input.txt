macro-reader [ ] (\ items items _)

// defn を定義
macro-ast defn (\ args (
  set-in get-macro-env head get-arg 0 head tail get-arg 0 
) _)

// comptime で計算を行う。
// 実行時には既に '100000' という値に置き換わっているはず。
defn [my-var comptime (:- tmp _ 100000)]

write my-var
